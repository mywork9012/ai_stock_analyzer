# ============================================================
# AI股票量化分析软件 V1.0 依赖清单 - Streamlit Cloud 最终部署版
# Python 3.14（Streamlit Cloud 硬锁定）
# ============================================================

# Web框架
streamlit>=1.36.0,<2.0.0

# 数据获取
baostock>=0.8.8
akshare>=1.12.0
tushare>=1.2.89
requests>=2.31.0

# 数据处理
pandas>=2.0.0,<3.0.0
# [修复] Python 3.14 只有 numpy 2.x 的预编译 wheel，移除 <2.0.0 上限
numpy>=2.0.0
pyarrow>=14.0.0

# 技术指标
# [移除] pandas-ta：依赖 numba，numba 硬限 Python <3.14，无法安装
# [无需替换] factor_engine.py 内置完整的纯 numpy/pandas 实现（_calc_builtin）
# 覆盖：SMA/EMA/RSI/MACD/布林带/ATR/OBV/KDJ/量比，功能完整

# 深度学习
torch>=2.0.0,<3.0.0
scikit-learn>=1.3.0

# 回测引擎
# [移除] vectorbt：内部依赖 numba，Python 3.14 下运行时崩溃
# [无需替换] backtest_engine.py 已内置手动回测引擎（_run_manual_backtest）
# 含：资金曲线/年化收益/Sharpe/MDD/Calmar/胜率/盈亏比，功能完整

# 可视化
plotly>=5.18.0
pyecharts>=2.0.4
streamlit-echarts>=0.4.0

# 可解释性
# [移除] shap：依赖 llvmlite，Python 3.14 下编译失败
# 原需求注释已说明：V1.0 暂用占位，V2.0 异步启用，移除不影响功能

# 数据库
sqlalchemy>=2.0.0

# 配置管理
pyyaml>=6.0
pydantic>=2.0.0

# 工具库
python-dateutil>=2.8.2
tqdm>=4.66.0
joblib>=1.3.0
