# ============================================================
# AI股票量化分析软件 V1.0 依赖清单 - Streamlit Cloud 部署版
# Python 3.11（由 .python-version 锁定）
# ============================================================

# Web框架
streamlit>=1.36.0,<2.0.0

# 数据获取
baostock>=0.8.8
akshare>=1.12.0
tushare>=1.2.89
requests>=2.31.0

# 数据处理
pandas>=2.0.0,<3.0.0
# Python 3.11 下 numpy 1.x 有预编译 wheel，保留上限保证 vectorbt 兼容性
numpy>=1.24.0,<2.0.0
pyarrow>=14.0.0

# 技术指标
# [修复] pandas-ta 0.4.x 强依赖 numba，numba 硬限制 Python <3.14
#        Python 3.11 环境下 numba 可正常安装，此行在 3.11 下可用
# [备用] 若仍报 numba 错误，可改为纯 Python 方案：ta>=0.10.2（无 numba 依赖）
pandas-ta==0.4.71b0

# 深度学习
torch>=2.0.0,<3.0.0
scikit-learn>=1.3.0

# 回测引擎
# 锁定 0.28.5，1.0.0 为破坏性升级
vectorbt==0.28.5

# 可视化
plotly>=5.18.0
pyecharts>=2.0.4
streamlit-echarts>=0.4.0

# 可解释性
# [修复] shap 0.51.0 依赖 llvmlite，llvmlite 不支持 Python 3.14
#        Python 3.11 下 shap + llvmlite 正常安装
# [原始需求注释] V1.0 暂用占位，V2.0 异步启用
shap>=0.44.0

# 数据库
sqlalchemy>=2.0.0

# 配置管理
pyyaml>=6.0
pydantic>=2.0.0

# 工具库
python-dateutil>=2.8.2
tqdm>=4.66.0
joblib>=1.3.0
